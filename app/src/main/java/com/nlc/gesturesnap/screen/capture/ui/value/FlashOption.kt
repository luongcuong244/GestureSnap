package com.nlc.gesturesnap.screen.capture.ui.value

import com.nlc.gesturesnap.R

enum class FlashOption(val icon : Int) {
    OFF(R.drawable.ic_flash_off),
    ON(R.drawable.ic_flash_on),
    AUTO(R.drawable.ic_flash_auto);
}